

#!/bin/bash

#the following command configures the name and folder of the produced core file
#in this the folder is the current directory
#after executing this command you must execute in the sheel the command: ulimit -c unlimited
sudo bash -c "echo 'core.%p.%e.%t.%h' >  /proc/sys/kernel/core_pattern"
